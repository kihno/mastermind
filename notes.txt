intersection = ["white", "white", "black", "blue"] - ["yellow", "white", "blue", "black"]

intersection = []

compring arrays with duplicate values doesn't work, find another method.

for each guess
    array.include guess? color += 1
